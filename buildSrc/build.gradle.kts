import org.gradle.kotlin.dsl.`kotlin-dsl`

/**
 * Gradle Kotlin DSL : Kotlin DSL provides support of using kotlin in gradle scripts.
 */
plugins {
    `kotlin-dsl`
}
repositories {
    mavenCentral()
}